{
  "name": "shiguangji",
  "version": "1.0.0",
  "private": true,
  "description": "拾光记 - 移动端记录软件（记账/任务/便签/心情/习惯/日记），单文件 PWA，包装为安卓 APK",
  "scripts": {
    "build:www": "mkdir -p www && cp index.html manifest.webmanifest sw.js www/",
    "sync": "npm run build:www && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
